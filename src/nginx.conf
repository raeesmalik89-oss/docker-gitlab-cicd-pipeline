
events {
    worker_connections 1024;
}

http {
    upstream app_servers {
        server app:3000;
    }

    server {
        listen 80;
        server_name localhost;

        location / {
            proxy_pass http://app_servers;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /health {
            proxy_pass http://app_servers/health;
        }
    }
}
Save and exit.

Step 16: Verify the files were created

bash
ls -la docker-compose.yml nginx.conf
Output:

text
-rw-r--r-- 1 toor toor 1098 Jan 15 11:52 docker-compose.yml
-rw-r--r-- 1 toor toor  635 Jan 15 11:52 nginx.conf
Step 17: Test docker-compose locally

bash
docker-compose up -d
Output:

text
Creating network "docker-cicd-lab_app-network" with driver "bridge"
Creating volume "docker-cicd-lab_mongo-data" with default driver
Creating volume "docker-cicd-lab_redis-data" with default driver
Creating docker-cicd-lab_mongo_1 ... done
Creating docker-cicd-lab_redis_1 ... done
Creating docker-cicd-lab_app_1   ... done
Creating docker-cicd-lab_nginx_1 ... done
I've successfully created the docker-compose configuration and tested it locally. Should I continue with creating environment files and updating the CI/CD pipeline?


